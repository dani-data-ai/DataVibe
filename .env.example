# IMPORTANT: All credentials must be provided at runtime
# Never commit actual credentials to version control

# LLM APIs (Optional - for real LLM integration)
# OPENAI_API_KEY=your-openai-api-key-here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Frontend Configuration (for production deployment)
# NEXT_PUBLIC_API_URL=https://your-backend-domain.com

# Supabase Configuration (for authentication)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Note: This application uses cloud-only architecture
# - No local database required
# - Database connections provided by user at runtime
# - All services accessed via HTTPS APIs only